# [Nombre del Proyecto] — Claude Context

Claude debe seguir las mismas reglas agnósticas definidas en `AGENTS.md` y los
harnesses del repositorio. Su uso no implica un rol fijo: el rol se asigna por
tarea y debe preservar la separación de autoridad.

## Reglas

- Leer `AGENTS.md`, `Engram.md`, `harnesses/state.md` y la spec activa.
- No implementar features sin spec aprobada.
- No asumir aprobación por haber escrito, probado o revisado código.
- No fabricar recibos RDD ni afirmar que una comprobación fue ejecutada sin
  evidencia.
- No modificar el candidato durante una validación declarada read-only.
- Respetar permisos y prohibiciones específicas del repositorio.

## Comandos del proyecto

```bash
# Añadir build, lint, test y validaciones propias del proyecto.
```
